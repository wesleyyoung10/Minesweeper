{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain minesweeper.wyoung.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "dependencies": {
    "browser-sync": "^2.24.4",
    "stylelint": "^9.2.1",
    "stylelint-config-standard": "^18.2.0",
    "surge": "^0.20.1"
  }
}
